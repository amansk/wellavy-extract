# Environment variables for blood test PDF extractor API
# Copy this file to .env.local for local development
# In Railway, set these directly in the environment variables section

# API Key for securing the AI extraction endpoint
# Generate a strong random key for production
API_SECRET_KEY=your-secret-api-key-here

# Anthropic API key for Claude
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# OpenAI API key (optional, only if using GPT-4o)
OPENAI_API_KEY=sk-your-openai-key-here

# Port for the API server (Railway will provide this)
PORT=8000